{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Seminaire list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sujet</th>
                <th>Datesem</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('seminaire_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sujet }}</td>
                <td>{% if entity.dateSem %}{{ entity.dateSem|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('seminaire_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('seminaire_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
                <td>{{ seminaires }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('seminaire_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
